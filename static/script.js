document.addEventListener("DOMContentLoaded", function () {
    // No sorting needed; images are already sorted on the server side
    console.log("Images are already sorted by last modified time on the server.");
});
